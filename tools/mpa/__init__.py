"""win103-byteexact: harness para decompilación byte-exact de Windows 1.03.

The internal Python package name `mpa` is kept as a legacy abbreviation
to avoid touching every import across the codebase; the distribution
name (see pyproject.toml) is `win103-byteexact`.
"""

__version__ = "0.1.0"
